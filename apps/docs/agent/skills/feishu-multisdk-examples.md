# Feishu Multi-SDK Examples

Installable skill source: `skills/feishu-multisdk-examples/SKILL.md`.

Workflow recipe:

```bash
md2feishu workflow show multisdk-examples --format json
```

Install team skills from the repository root:

```bash
scripts/install-codex-skills.sh --remove-legacy
```
