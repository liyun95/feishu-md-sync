# Feishu SDK Reference Authoring

Installable skill source: `skills/feishu-sdk-reference-authoring/SKILL.md`.

Workflow recipe:

```bash
md2feishu workflow show sdk-reference-authoring --format json
```

Install team skills from the repository root:

```bash
scripts/install-codex-skills.sh
```
