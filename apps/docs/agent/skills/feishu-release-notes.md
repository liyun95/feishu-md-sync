# Feishu Release Notes

Installable skill source: `skills/feishu-release-notes/SKILL.md`.

Workflow recipe:

```bash
md2feishu workflow show release-notes --format json
```

Install team skills from the repository root:

```bash
scripts/install-codex-skills.sh
```
