# Feishu SDK Reference Release

Installable skill source: `skills/feishu-sdk-reference-release/SKILL.md`.

Workflow recipe:

```bash
md2feishu workflow show sdk-reference-web-content-release --format json
```

Install team skills from the repository root:

```bash
scripts/install-codex-skills.sh
```
